steps:
  - name: gcr.io/cloud-builders/docker
    env:
      - DOCKER_BUILDKIT=1
    args:
      - build
      - -t
      - gcr.io/$PROJECT_ID/ckdesign-room
      - .

images:
  - gcr.io/$PROJECT_ID/ckdesign-room

options:
  logging: CLOUD_LOGGING_ONLY
